import {InjectionToken} from '@angular/core';
import {IDialogSettings} from '@cms-ui/core';

// Dialog settings
export const BS_DIALOG_SETTINGS_PROVIDER =
  new InjectionToken<IDialogSettings>('BS_DIALOG_SETTINGS_PROVIDER');
